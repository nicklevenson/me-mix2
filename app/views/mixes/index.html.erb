<%=render partial: 'users/user', locals: {user: @user}%>

<h2>Mixes  <%=new_link%></h2> 
<%@mixes.each do |m|%>
  <div class="mix-container">
    <%=render partial: 'mix', locals: {mix: m}%>
  </div>
<%end%>